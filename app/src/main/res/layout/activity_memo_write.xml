<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="#bdbdbd"
    tools:context="com.example.recyclerview.MemoWrite">

    <EditText
        android:id="@+id/MemoWriteTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="제목"
        android:background="#ffffff"
        android:textSize="20sp" />

    <ImageView
        android:id="@+id/MemoWriteImage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/MemoWriteTitle"
        android:layout_marginTop="10dp" />

    <EditText
        android:id="@+id/MemoWriteBody"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/MemoWriteImage"
        android:layout_marginTop="10dp"
        android:hint="여기에 메모를 작성하세요."
        android:background="#ffffff"
        android:gravity="top"/>

</RelativeLayout>
